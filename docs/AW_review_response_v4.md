# AW comments on UNC tool — response (v4)

**Source comments:** `AW comments on UNC tool 1_5_26.pdf`
**App version reflected:** post Round 4 (full IPCC variable rename) + Round 5 current-version fixes
**Live deploy:** https://mlolita26.shinyapps.io/cattle-ghg-uncertainty/
**Last updated:** May 2026

This document mirrors the structure of Andreas's PDF: every comment from every section appears with a plain-language note on how it was addressed. Items still open are marked **Awaiting input** or **Noted, deferred to v4.0** with a one-line reason.

---

## Opening page

| Comment | How it was addressed |
|---|---|
| 1. Minor edits: edits to the phrasing needed to make it more specific / technically correct, but I assume these edits are cosmetic, so let's deal with that after the main tool programming. | Noted. Will be batched as a final wording pass at the end of the programme. |
| 2. Suggestion: Either in this tab or add a new tab called 'Useful resources' that links to basic knowledge about Tier 2 (IPCC Guidelines), activity data inputs (L-ADG), uncertainty analysis (IPCC, case studies…). | Done. Added a Resources nav panel between Home and Data Input with links to IPCC 2006 Vol. 4 Ch. 10–11, the 2019 Refinement, IPCC 2006 Vol. 1 Ch. 3 (uncertainties), the FAO L-ADG, Penman et al. (2000), Monni et al. (2007), Frey & Rhodes, Karimi-Zindashty, and Milne. (R/app_ui.R) |
| 3. Suggestion: It's fine to work with the Uganda example for now, but let's anonymize it / change it to a hypothetical country for the beta testing versions. ANONYMIZE THE TEMPLATE AS WELL. | Done. Dropdown labels now read "Country X (hypothetical dairy)" and "Country Y (hypothetical pastoral)". The Excel template was fully anonymised in Round 4 — README, Inventory_Metadata defaults, time-series example, and Manure_Management instructions all stripped of Uganda references. (R/app_ui.R, R/app_server.R, R/utils_template.R, R/utils_ipcc_defaults.R) |
| 4. Add a statement of what is needed before this tool can be used (and maybe what this tool cannot do, e.g. estimate uncertainties of inputs). Again, minor edit, so can be addressed later. | Done. New "Before you start" card on the Home page lists prerequisites (Tier 2 dataset, uncertainty estimates, optional time-series) and a "What this tool does NOT do" section (does not collect data, does not produce Tier 1, does not validate IPCC categorisation, no cross-block correlations yet). (R/app_ui.R) |

## Tab 1 — Data input

| Comment | How it was addressed |
|---|---|
| 1. Possible bug? Custom upload seems not to be working. It kept showing the Uganda default values. | Done. Two combined causes: (a) upload errors were written to the Tab-5 log so the user on Tab 1 never saw them — now every upload result fires a `showNotification` plus a banner in the validation panel; (b) `readxl::read_excel` defaulted to skip = 0 and treated the banner row as column names — a new `read_sheet_smart()` tries skip = 0..3 and picks the offset where expected column names appear. The country dropdown now also asks for confirmation before overwriting custom data. (R/app_server.R, R/utils_template.R) |
| 2. If there is a custom upload, but one of the parameters is missing, what happens? Is there an automatic check for completeness (i.e., for each sub-category defined, all required parameters have parameter values and distributions)? | Done. `ensure_completeness()` auto-fills missing core parameters from `PARAM_CATALOGUE` defaults rather than blocking the Run button, and flags imputed rows with `imputed = TRUE`. Imputed rows render in red bold via `DT::formatStyle`, an "Imputed parameters" summary card appears on Tab 5, and a QA/QC warning lists every imputed row so the user can decide to override. (R/utils_validation.R, R/utils_qaqc.R, R/app_server.R) |
| 3. Parameter names and abbreviations — check consistency with IPCC software, if relevant. We don't have to follow 100%, but the closer we are, the fewer questions we'll get from reviewers. We need to actually change the names of the variable to the IPCC standards. REMOVE THE DEFINITIONS SCREENSHOT PROVIDED BY ANDREAS WILKES. | Done. Round 4 R1.6: the variables in the app and the Excel template now ARE the IPCC names. Renamed `cattle_pop`→`N`, `live_weight`→`W`, `mature_weight`→`MW`, `weight_gain`→`WG`, `milk_yield`→`Milk`, `milk_fat`→`Fat`, `protein_milk`→`MilkPR`, `C_growth`→`C` (on top of the earlier rename of `ash`→`ASH`, `DE_pct`→`DE`, `CP_pct`→`CP`, `Ym_pct`→`Ym`, `Frac_GASM`→`Frac_GASMS`, `Frac_LEACH`→`Frac_LEACH_H`). `PARAM_ALIASES` preserves backwards compatibility for legacy templates. The Definitions tab no longer carries a screenshot — variable names ARE the IPCC names, so the cross-reference column was removed in R1.7. (R/utils_template.R, all R/calc_*.R, R/mc_simulation.R) |
| 4. Parameter abbreviations — need a link somewhere to a list of definitions, perhaps in a 'definitions' tab. | Done. Definitions nav panel renders the 24-row `PARAM_CATALOGUE` as a sortable DT with definition, unit, IPCC default, suggested distribution, tier, IPCC framing, and IPCC reference. Per Round 4 feedback, the IPCC software name column was dropped (variables now ARE the IPCC names) and the classification mapping table was removed. (R/app_ui.R, R/app_server.R) |
| 5. Parameter type: 'emission factor' should be changed to something else like 'coefficient' because AD and these coefficients are used together to calculate EFs. In IPCC uncertainty analysis reports, AD refers to population, and EF refers to emissions per head per year. | Done. Round 3 D1: `param_type = "emission_factor"` was renamed to `"coefficient"` everywhere. After the rename, only `N` (population) is `activity_data`; all 23 other parameters are `coefficient`. `parse_uploaded_template()` aliases legacy `"emission_factor"` → `"coefficient"` on upload so old templates keep working. (R/utils_template.R, R/utils_validation.R, R/mc_uncertainty.R, all R/calc_*.R) |
| 6. Guidance: For users, this page needs some guidance, especially to clarify that 'lower' and 'upper' refer to (pre-calculated) 95% CI bounds given in the same units as the parameters and that they are automatically calculated if uncertainty_pct is updated. uncertainty_pct needs a clear definition. | Done. New explainer paragraph at the top of Tab 1: `mean` is the central estimate; `uncertainty_pct` is the symmetric ±% half-width of the 95% CI; `lower` and `upper` are the absolute 95% CI bounds in the same units. Editing any of these auto-updates the others. (R/app_ui.R, Tab 1 info-panel) |
| 7. For triangular distributions, may need guidance on how to convert min/max to lower/upper 95% CI bounds, as this distribution type is mainly used when only min, most likely and max are known. Guidance can be in a separate doc, so not crucial to address now. | Done. Inline guidance added at the top of Tab 3: "triangular treats lower/upper as absolute min/max; if you have 95% CI bounds, use PERT instead." (R/app_ui.R) |
| 8. Possible bug? Editing uncertainty_pct updates lower/upper, but editing lower/upper does not update uncertainty_pct. Ideally the tool would automate consistency in the editing. | Done. The `param_table_cell_edit` observer was rewritten with bidirectional logic: editing `mean` or `uncertainty_pct` recomputes `lower`/`upper` from the symmetric half-width; editing `lower` or `upper` recomputes `uncertainty_pct` from the implied half-width. (R/app_server.R) |
| 11.a. Possible bug? If I change an IPCC coefficient to 'constant', the lower and upper bounds are not adjusted unless I also change uncertainty_pct to '0'. | Done. Same observer rewrite: when `distribution = "constant"`, `uncertainty_pct` is auto-set to 0 and `lower = upper = mean`. (R/app_server.R) |
| 12. Emission sources included: Tier 2 typically covers enteric, manure CH4, manure N2O and pasture deposit N2O, but some users may only want a subset. Does the tool just ignore incomplete data, or could the opening page select which emission sources will be included? Make this MORE OBVIOUS for the user — default = nothing ticked, Run blocked if nothing ticked. | Done. `checkboxGroupInput` on Tab 5 lists the five sources (enteric CH4 / manure CH4 / manure N2O direct / manure N2O indirect / pasture N2O). Per beta-tester pass 2, default selection is `character(0)` (nothing ticked); the Run button surfaces an explicit error if zero are ticked. After simulation, the totals (`total_ch4`, `total_n2o`, `total_co2e`) are recomputed from only the selected sources; per-source columns are kept so sensitivity and decomposition still work. (R/app_ui.R, R/app_server.R) |

## Blank Template

| Comment | How it was addressed |
|---|---|
| 1. Data entry: will need to clarify (e.g. with examples) how to enter the pct data values (e.g. '45' or '45%'). | Done. Parameters-sheet instruction banner now states: "PERCENTAGE FIELDS: enter the bare number, e.g. '45' for 45% — do NOT include the '%' symbol. uncertainty_pct is the ±% half-width of the 95% CI." (R/utils_template.R) |
| 2. 'Data quality' — suggest to delete this column if it has no function in the calculations. (Data quality is multidimensional and is addressed in more detail in L-ADG.) | Done. `data_quality` column removed structurally — from `P_COLS`, the widths array, the per-row data builder, the legend labels, the dropdown validation, the Vocab section, and from the `generate_template_basic` fallback. (R/utils_template.R) |
| 3. MMS dropdown list: If I select IPCC 2019, the dropdown for manure management options is not the 2019 list of options; if I select IPCC 2006 the list is the same; the list does not include all the dairy and other cattle options in either version. | Done — fully addressed in **Round 7.1** (the initial Round 4 fix expanded the MMS coverage but kept a single combined dropdown, deferring version mismatches to upload-time validation; 7.1 makes the dropdown itself version-aware). Three changes: (a) `MMS_DEFAULTS` expanded from 8 to 12 entries with a `versions` column noting which IPCC editions recognise each system — new 2019-only entries: `solid_storage_covered`, `anaerobic_digester`, `aerobic_treatment`, `burned_for_fuel`. (b) The Excel template's MMS dropdown now filters to the version chosen at download time. Tab 1 has a radio "IPCC Guidelines version for template" (2006 / 2019_refinement); the chosen version is passed to `generate_template(ipcc_version = …)` which sources the dropdown from `get_mms_for_version(ipcc_version)$id` and pre-sets the `ipcc_version` cell in Inventory_Metadata to match. Download filename also includes the version (`uncertainty_template_ipcc2006.xlsx` / `_ipcc2019.xlsx`). Verified via dropdown-range inspection: 2006 template → `'_Lists'!$E$2:$E$9` (8 entries), 2019 template → `'_Lists'!$E$2:$E$13` (12 entries). (c) `validate_manure_sheet(manure_df, meta)` remains version-aware as a defensive backstop for users who manually edit the dropdown source. (R/utils_template.R, R/utils_ipcc_defaults.R, R/utils_validation.R, R/app_ui.R, R/app_server.R) |
| 4. Manure management coefficients and EFs: EF4, EF5, Frac_LEACH, Frac_GASMS, Frac_LEACH_H and Frac_GASMS are missing columns. | Done in Round 7 (= R1.12 / R1.13 below). The Manure_Management sheet now carries per-MMS `Frac_GasMS_pct` and `Frac_LeachMS_pct` columns populated from IPCC 2019 Refinement Tables 10.22 / 10.23, and `calc_indirect_n2o_mm()` was rewritten to sum across MMS types. EF4 and EF5 stay broadcast scalars per IPCC 2019 (they don't vary by MMS). (R/utils_template.R, R/utils_ipcc_defaults.R, R/calc_manure_n2o.R) |
| 5. Data entry for MMS: Often, the same values will be used for all sub-categories in a system. Guidance should be clear as to whether the same values must be repeated per sub-category, or whether one row per system applies to all sub-categories. | Done. Manure_Management instruction banner now states: "if all sub-categories of the same cattle_type+aggregation_level use the same MMS allocation, leave sub_category blank — values apply to every sub-category in that group; otherwise provide separate rows per sub-category." This same broadcast guidance is also surfaced in the Tab 1 info-panel (R1.9). (R/utils_template.R, R/app_ui.R) |
| 6. User options: IPCC Guidance is to apply uncertainty analysis to the first and last year of the inventory and to calculate uncertainty of the trend. However, users may want to do uncertainty analysis just for one year. Could the initial pages give an option? | Done. Round 4 R1.11 added a "Single year vs Trend" radio on the Home page. Selecting "Trend" + Run redirects to Tab 9 (Trend) instead of running a single-year simulation. (R/app_ui.R, R/app_server.R) |
| 7. Parameter time series: see comments in the 'correlations' tab below — what about correlations not based on time series? | Done in Round 7 (= R1.15 below). Resolved by expanding the IPCC-guidance preset rather than adding a fifth source. Two changes: (a) bug fix so the preset actually applies (it was operating on AD names which post-R1.6 left only `N`, making the matrix 1×1); (b) two new pairs added — Cfi↔Ca (0.60) and N↔W (0.30). Tab 4's four sources (time-series auto / IPCC-guidance preset / manual CSV / none) are unchanged in count; the preset is now the substantive non-time-series option. Expert-elicitation upload remains a v4.x candidate if a future user asks. (R/mc_sampling.R, R/app_server.R) |
| 8. Can we help the user by adding some kind of QC in the data input template to ensure completeness and so they can document that they have checked the inventory parameter values, distributions and uncertainty_pct values before upload? | Done. Round 3 G3 added Excel-level conditional formatting in the generated template: red on blanks in required cells, red on out-of-vocab distribution values, orange on `uncertainty_pct > 100`, red on `lower > upper` (both bound cells). At runtime, `validate_completeness()` (Tab 1 #2) and the upload-error notifications (Tab 1 #1) cover the same surface for users who edited the file outside the template tooling. (R/utils_template.R, R/utils_validation.R) |
| 9. Parameter type is set by default, not user-defined, and Tab 3 states that 'emission factor' parameters will be sampled independently. See comments in correlations tab below about situations where 'emission factor' coefficients may be correlated with other activity data inputs. | Done in Round 7 (= R1.16). Tab 3's `uncertainty_table` DT now persists `param_type` edits back into `rv$param_specs` with validation (`activity_data` / `coefficient` only). Cross-block correlations are also addressed via T4.3 — the unified 24-parameter copula now allows AD↔coefficient pairs like N↔W. (R/app_server.R, R/utils_qaqc.R, R/mc_sampling.R) |

## Tab 2 — QA/QC

| Comment | How it was addressed |
|---|---|
| 1. Benchmark deviation: how does this work, e.g. if a country-specific category is defined (e.g. 'tollies'), what does it benchmark against in the IPCC categories? Do the benchmarks consider all IPCC regions? Do the benchmarks change between IPCC 2006 and IPCC 2019? | Done (region-aware benchmarks). Round 3 G2 added `IPCC_DEFAULTS_BY_REGION` covering africa / asia / europe / americas / oceania / global × {W, Milk, DE, Ym, Bo}, sourced from IPCC Vol. 4 Tables 10A.1, 10A.2, 10.4, 10.7. `run_qaqc()` now takes a `region` argument and overrides the global default through `get_regional_default()`. For country-specific categories (e.g. 'tollies') with no IPCC analogue, the benchmark check falls back to the closest available default and writes "deviation from IPCC default" so the inventory compiler can document the comparison. The 2006 vs 2019 split for benchmark *values* (most tables are unchanged across editions) is currently a single combined table — flagging genuine 2019-only revisions is on the v4.0 list. A region-aware *completeness* layer (param X is required for region Y but not Z) is also v4.0 work. (R/utils_qaqc.R, R/utils_ipcc_defaults.R) |
| 2. Is there a completeness check (e.g. for each defined sub-category, a minimum set of variables must be characterised)? | Done. Same implementation as Tab 1 #2 — `ensure_completeness()` auto-fills missing core parameters and flags them as imputed; the user is shown which rows were filled and from where. (R/utils_validation.R) |
| 3. It's quite likely that a user may select a continuous distribution (e.g. normal) for some values that have a maximum or minimum value, e.g. proportions. Does or could the tool implement checks or automated cut-offs for fractional parameters, so that impossible simulation combinations do not occur? | Done. New QA/QC check `fraction_distribution` in `run_qaqc()`. For each parameter in `FRACTION_PARAMS` (`pct_lactating`, `ASH`, `UE`, `Frac_GASMS`, `Frac_LEACH_H`), a warning fires if the distribution is one of `normal`, `lognormal`, or `posnorm` — the row's message recommends `tnorm_0_1` or `beta` instead. Automated sample clipping is not applied; the QA/QC layer surfaces the issue and lets the user pick a better distribution explicitly. (R/utils_qaqc.R) |

## Tab 3 — Uncertainty

| Comment | How it was addressed |
|---|---|
| 1. If a user edits the values in this tab, are the automated QC checks in Tab 2 also applied? | Done (already working). The `qaqc_result` reactive depends on `rv$param_specs`; any cell edit on Tab 3 (which writes to `rv$param_specs`) automatically retriggers QA/QC. (R/app_server.R) |
| 2. It states "Use the quick-set buttons at the bottom to apply common settings to all parameters of one type". I cannot find these buttons. | Done. Two `observeEvent` handlers added in Phase 1: `set_all_normal` sets all activity-data parameters to `normal` with `uncertainty_pct = 15`, recomputing bounds; `set_all_pert` sets all coefficient parameters to `pert`. **Round 5 R2.1** also moved the buttons out of the table card body into the card_footer so they remain visible regardless of how tall the table is — beta testers reported they were hidden below the DT. Labels now read "Set all activity-data params to Normal ±15%" and "Set all coefficients to PERT". (R/app_ui.R, R/app_server.R) |
| 3. See comments about correlations of 'emission factor' parameters with other parameters in correlations section below. | Cross-reference — see Tab 4 #3 below. |

## Tab 4 — Correlations

| Comment | How it was addressed |
|---|---|
| 1. AD correlations: This calculates correlations based on time series. What about other correlations, e.g. if inventory value for milk yield was calculated using the assumptions for calf daily weight gain, then these 2 parameters should be correlated. Would this correlation necessarily be the same as the time series correlation? | Done. Tab 4 now offers four modes: `none` (default), `From template (auto)` (computes a Pearson matrix from the Parameter_TimeSeries sheet, then projects to nearest positive-definite via `Matrix::nearPD`), `IPCC-guidance preset` (a sparse matrix containing only well-documented structural pairs — W ↔ MW, Milk ↔ Fat, DE ↔ CP, etc.), and `Advanced — manual entry` (CSV upload of a full square matrix). Time-series mode also has a group-scope selector: "all AD / population only / intake only". The four modes correspond to four different physical assumptions, so a user with the milk_yield ↔ calf_weight_gain example can choose the preset or manual mode rather than time-series. **Round 5 R2.2** also fixed a bug where the built-in examples produced a "no time-series data" warning even though they were meant to demonstrate this feature — Country X and Country Y now ship with five years of synthetic time-series so this mode works without requiring an upload. (R/app_ui.R, R/app_server.R, R/utils_ipcc_defaults.R, R/utils_template.R) |
| 2. Guidance on correlations will be needed. | Done. Tab 4 info-panel now has an expanded "When to use correlations" section with bullets for each mode and concrete recommendations for when to choose each. (R/app_ui.R) |
| 3. EF correlations: Some coefficients (e.g. Cfi) may be calculated using data on proportion of cows giving birth, so this AD and 'EF' (or coefficient) may be correlated. How to capture this? | Done in Round 7 (T4.3). The two independent copulas were unified into a single Gaussian copula spanning all parameters. `generate_mc_samples()` accepts a new `unified_corr_matrix` argument that holds the AD block, the coefficient block, **and** the cross-block (AD↔coefficient) entries. The IPCC-guidance preset adds a documented cross-block pair `N↔W = 0.30` so the unified path has at least one pair active by default; users can also upload a full unified matrix via Tab 4's manual CSV mode. Verified end-to-end: sampling with the preset reproduces `cor(N, W) ≈ 0.30` in the MC output. (R/mc_sampling.R, R/mc_simulation.R, R/app_server.R) |
| 4. It states "Default is no EF correlation, which is the standard IPCC Approach 2 assumption." Is this correct? | Done. The wording overstated the IPCC position. The Approach 2 recommendation is to incorporate correlations where known, not to assume zero. Tooltip rewritten: "Default is no EF correlation — a simplifying assumption used when no information on correlations is available. IPCC Approach 2 recommends incorporating known correlations where they exist." (R/app_ui.R) |
| 21. Correlations: for MMS, IPCC 2019 Box 3.1A gives specific recommendation to consider the correlation between MMS and to constrain the uncertainties as they are mutually dependent. How to address this in the tool? | Noted, deferred to v4.0. v4.0 will replace the current independent per-MMS sampling with a Dirichlet draw so the MMS shares sum to 100% in every Monte Carlo iteration, per IPCC 2019 Box 3.1A. The Dirichlet's concentration parameters will be derived from the user-supplied mean fractions and uncertainty bounds via method-of-moments. |
| 22. Correlations when calculating inventory trend: IPCC 2019 page 3.22 onwards refers to situations in which EF (or EF input parameters) and AD are fully, partially or not correlated; some guidance on how to determine this is given on page 3.28 ("it is suggested to assume no correlation between years if the estimates for the two years are independent from each other, for example based on independent measurement"). What are the implications of the IPCC guidance for the design of the correlations part of the tool? | Noted, deferred to v4.0. v4.0 Trend tab will gain three between-year modes per IPCC 2019 §3.22: `independent` (current default — independent measurement), `fully_correlated` (shared random seed across years, only the means shift), and `partially_correlated` (user-supplied year-to-year correlation, default 0.5, applied via Gaussian copula across the year dimension). |

## Tab 5 — Simulate

| Comment | How it was addressed |
|---|---|
| 1. Would be good if the tool automatically proceeds to Tab 6 when it's complete. SOMETHING BETTER THAN SCROLLING DOWN? | Done. After a successful run, `bslib::nav_select(id = "nav", selected = "5. Simulate & Results")` switches the view in-place. Round 4 R1.5 went further: Simulate and Results were merged into a single tab with a settings/results flip via `rv$sim_view`, so the user no longer scrolls — the settings card hides, the results render in the same tab, and a "← Back to settings" button restores the input view. (R/app_ui.R, R/app_server.R) |

## Tab 6 — Results

| Comment | How it was addressed |
|---|---|
| 1. Summary cards: This presents CV (which I assume is sd/mean). IPCC guidance is to summarise uncertainty as a 95% margin of error. | Done. The "Total CO2eq" value box was replaced with a 95% Margin of Error card. The metric is computed quantile-based (`((q975 − q025) / 2) / mean × 100`) so it handles asymmetric distributions correctly. This also fixed a latent bug: the previous `moe_pct` field used `1.96 × sd / sqrt(n) / mean` — the standard error of the estimator, which is ~1/100 of the true MoE for n = 10,000. CV% is retained as a secondary card. (R/mc_uncertainty.R, R/app_ui.R, R/app_server.R) |
| 2. Summary categories: Rather than report per gas, report per inventory reporting category (e.g. dairy enteric, other cattle enteric, dairy manure CH4, …). | Done. New "By Reporting Category" card on Tab 6 cross-tabs each system × source (Enteric CH4 / Manure CH4 / Manure N2O direct / Manure N2O indirect / Pasture N2O direct / Pasture N2O indirect) with mean t CO2eq, 95% MoE, CV%, and CI bounds. (R/app_ui.R, server output `results_by_category` in R/app_server.R) |
| 3. CO2eq is not needed in the summary, but useful to retain in calculations if sensitivity includes the option to assess parameters related to all emission sources. | Done. CO2eq removed from the primary value-box row (replaced by 95% MoE) and shown as a small inline line below the cards. Underlying `mc_results$inventory$total_co2e` is unchanged, so sensitivity and decomposition still work. (R/app_ui.R) |
| 4. Decomposition: I'm guessing the definitions of activity data and emission factor used in this decomposition differ from the IPCC definitions. Ideally, align with the IPCC whereby AD = population, EF = calculated emission factor. | Done. Round 3 D1 restructured the decomposition. `decompose_uncertainty()` now varies only `N` for the AD-only run and only the 23 coefficients for the EF-only run, matching the IPCC convention. Chart labels read "Activity Data uncertainty" and "Coefficient uncertainty" (or "Emission factor uncertainty" on the IPCC Report tab where Andreas's wording is preferred). (R/mc_uncertainty.R, R/app_ui.R, R/app_server.R) |

## Tab 7 — Sensitivity

| Comment | How it was addressed |
|---|---|
| 1. Clarify if the results identify parameters with higher influence on overall uncertainty or higher influence on the level of emissions. | Done. Explicit note above the tornado chart: "These rankings show which parameters drive the **uncertainty** of total emissions, not the absolute emission level." (R/app_ui.R) |
| 2. Ideally, there is one chart and one output table per emission source (e.g. enteric, manure mgt CH4) and the option of a chart combining all emission sources. | Done. `selectInput` "Output variable" on Tab 7 with seven choices: Total CO2eq (all sources) plus one per source (Enteric CH4 / Manure CH4 / Manure N2O direct/indirect / Pasture N2O direct/indirect). The `active_sensitivity()` reactive reruns `sensitivity_analysis()` against the selected output column using the cached MC samples; the precomputed `rv$sensitivity` is served instantly for the default Total CO2eq case. (R/app_ui.R, R/app_server.R) |

## Tab 8 — IPCC report

| Comment | How it was addressed |
|---|---|
| 1. I assume CV is sd/mean, but IPCC requires to report 95% CI margin of error. | Done. Same implementation as Tab 6 #1 — the by-system and IPCC tables now include a `MoE_95_pct` column alongside CV%. (R/mc_uncertainty.R, R/app_server.R) |
| 2. AD and EF uncertainty: I'm guessing the definitions of activity data and emission factor used in this decomposition differ from the IPCC definitions. Ideally, align with the IPCC whereby AD = population, EF = calculated emission factor. | Done. Same implementation as Tab 6 #4 — Round 3 D1 restructured AD/EF so AD = population only (`N`) and EF = the 23 coefficients combined. The IPCC Report tab carries an explicit callout: "in this version, AD = population uncertainty only (N), and EF = the per-head emission factor uncertainty driven by the 23 coefficients (live weight, feed quality, Ym, Bo, Frac_GASMS, etc.). This matches IPCC Volume 1 Chapter 3 reporting conventions." (R/mc_uncertainty.R, R/app_ui.R) |
| 3. Uncertainty report: The results are shown, but the inputs are in a separate file, and some editing may have been done in the tool. Would it be possible also to add a report tab summarising all inputs (system/sub-category, parameter, distribution, lower/upper bounds…)? This way, the full documentation of each run is in one place. | Done. New "Input parameters used in this run" card on Tab 8 renders a DT with one row per parameter and columns: cattle_type, aggregation_level, sub_category, parameter, param_type, mean, uncertainty_pct, lower, upper, distribution, data_source, ipcc_ref. Captures both the original template and any edits made in the app. (R/app_ui.R, server output `inputs_doc_table` in R/app_server.R) |
| 4. Uncertainty report: For really basic users, is it possible to automatically generate the charts for the uncertainty of each emission source, and the tornado charts? | Done. Three new cards on Tab 8: (a) per-source histograms (5-panel `subplot` of Enteric CH4 / Manure CH4 / Manure N2O direct / Manure N2O indirect / Pasture N2O); (b) embedded tornado chart for Total CO2eq; (c) input distribution density plots (up to 12 params in a 4×3 panel) for third-party QA verification. (R/app_ui.R, server outputs `report_source_histograms`, `report_tornado`, `report_input_densities` in R/app_server.R) |

## Transcript items (meeting with Peter Steward and Lolita)

| Comment | How it was addressed |
|---|---|
| Andreas — show the fitted distribution in a final report output, useful for third-party QA review. | Done. Implemented as part of T8.4 — the "Input distributions used" card on Tab 8 shows density plots of each input parameter's MC samples, confirming each parameter was sampled with the marginal distribution specified in the input table. (R/app_server.R) |
| Lolita wanted to delete manual correlation entry; Andreas wanted to keep some flexibility (e.g. correlate population only). | Done (hybrid). Manual matrix entry is kept but moved under the "Advanced — manual entry" mode in Tab 4. Time-series mode gained a group-scope selector ("all AD / population only / intake only"). A new "IPCC-guidance preset" mode fills a sparse matrix of structurally documented pairs. (R/app_ui.R, R/app_server.R) |

## Round 4 — beta-tester pass 2 items

| Item | How it was addressed |
|---|---|
| R1.1 Time-series correlation matrix was being silently discarded after the AD/EF restructure (`N` is the only AD post-D1, so there was nothing to correlate). | Done. Correlation matrix now applies to the coefficient block where structural correlations actually live. `coef_names <- sys_specs$parameter[sys_specs$param_type == "coefficient"]; expand_corr_matrix(rv$corr_matrix, coef_names)`. (R/app_server.R) |
| R1.2 Excel template fully anonymised — all "Uganda" / "MAAIF Uganda" / "Eastern Uganda – pastoral" replaced with Country X / generic placeholders. | Done. (R/utils_template.R, R/utils_ipcc_defaults.R) |
| R1.3 Imputation visibility — `imputed` column on `param_specs`; auto-filled rows render in red bold; new "imputation" QA/QC entry warns on every imputed row; "Imputed parameters" summary card on Tab 5. | Done. (R/utils_validation.R, R/utils_qaqc.R, R/app_server.R) |
| R1.4 Emission source checkboxes default to none; Run is blocked with explicit error if zero ticked. | Done. (R/app_ui.R, R/app_server.R) |
| R1.5 Simulate ↔ Results flip with "← Back to settings" button. `output$sim_view` toggles between settings and results panels in Tab 5. | Done. (R/app_ui.R, R/app_server.R) |
| R1.6 Full IPCC variable rename (`cattle_pop`→`N`, `live_weight`→`W`, `mature_weight`→`MW`, `weight_gain`→`WG`, `milk_yield`→`Milk`, `milk_fat`→`Fat`, `protein_milk`→`MilkPR`, `C_growth`→`C`). PARAM_ALIASES auto-translates legacy templates. | Done. (R/utils_template.R, all R/calc_*.R, R/mc_simulation.R) |
| R1.7 Definitions tab simplified — "IPCC Inventory Software" column dropped (variable name now IS the IPCC name); "Our column" renamed "Variable name". | Done. (R/app_ui.R, R/app_server.R) |
| R1.8 Removed IPCC classification mapping table (Sector / Category / Geographical zone / Subdivision) from Definitions tab. | Done. (R/app_ui.R) |
| R1.9 MMS sub-category broadcast guidance prominent in Tab 1 info-panel. | Done. (R/app_ui.R) |
| R1.10 MMS validation now version-aware via `validate_manure_sheet(manure_df, meta)` — invalid MMS for the chosen IPCC version are flagged. | Done. (R/utils_validation.R) |
| R1.11 Single-year vs Trend radio on Home tab; selecting "trend" + Run redirects to Tab 9. | Done. (R/app_ui.R, R/app_server.R) |
| R1.12 Per-MMS column structure (EF4, EF5, Frac_LEACH per-MMS variants from IPCC 2019). | Done in Round 7. Manure_Management sheet extended from 14 to 22 columns adding `Frac_GasMS_pct` / `Frac_LeachMS_pct` plus `lower_*` / `upper_*` / `distribution_*` per side, populated with IPCC 2019 Table 10.22 / 10.23 defaults via the new `mms_frac_defaults_2019()` helper. EF4 / EF5 stay broadcast scalars (IPCC 2019 keeps them invariant by MMS). (R/utils_template.R, R/utils_ipcc_defaults.R, R/utils_validation.R) |
| R1.13 Per-MMS Frac variants — `calc_indirect_n2o_mm` rewrite. | Done in Round 7. Function signature now accepts `frac_gas_values` / `frac_leach_values` named vectors keyed by `mms_type` and sums ΣmmsNex × frac(mms) × Frac_GasMS(mms) × EF4 × 44/28 (and the leach term). NA per-MMS values fall back to IPCC 2019 defaults. Bridge in `app_server.R` extracts the new columns from the Manure_Management sheet and threads them through `run_mc_simulation()` → `ghg_emissions()`. Old broadcast signature kept as a deprecated wrapper. (R/calc_manure_n2o.R, R/calc_ghg_master.R, R/mc_simulation.R, R/app_server.R) |
| R1.14 Trend correlations — IPCC 2019 §3.22 fully / partially / not correlated between years. | Done in Round 7. New `radioButtons("year_corr", …)` on Tab 9 with three options (`full` / `partial` / `none`), default = `full` per IPCC 2019 §3.2.2.4. `run_trend_analysis()` accepts a `year_corr` arg: `full` pre-samples the coefficient block once and reuses it across all years (only AD redrawn per year); `partial` builds AR(1) draws across years per coefficient with ρ=0.7; `none` keeps the pre-Round-7 independent-year behaviour. New helpers `.pre_sample_coefficients()` and `.ar1_samples_one_coef()` in `R/trend_tab.R`. (R/trend_tab.R, R/app_ui.R, R/app_server.R, R/mc_sampling.R) |
| R1.15 Correlations not based on time series — what alternative source? Expert elicitation upload? Cross-parameter from inventory documentation? | Done in Round 7 via the expanded IPCC-guidance preset. Two changes: (1) bug fix — the post-R1.6 preset matrix was being built on `activity_data` parameter names which after the rename leaves only `N`, so the documented pairs (W↔MW, Milk↔Fat, …) silently never fired; preset now operates on the unified parameter set. (2) Two new pairs added: **Cfi↔Ca = 0.60** (structural breed/physiology) and **N↔W = 0.30** (cross-block AD↔coefficient via T4.3). Pure helper `build_ipcc_preset_corr(all_param_names)` in `R/mc_sampling.R` replaces the inline observer code so the pair list is testable. Expert-elicitation upload remains available as a v4.x option if a user requests it. (R/mc_sampling.R, R/app_server.R, R/app_ui.R) |
| R1.16 `param_type` user-overridable from the UI. | Done in Round 7. The Tab 3 `uncertainty_table` DT now persists edits (including `param_type`) back into `rv$param_specs` via a new `observeEvent(input$uncertainty_table_cell_edit, ...)` with the same bound-cascade rules as the Tab 1 DT. Edits to `param_type` validate against `c("activity_data", "coefficient")` (with `emission_factor` legacy alias accepted) — invalid values are rejected with a notification toast. New defensive QA/QC check `param_type_invalid` flags any uploaded rows with non-canonical values. (R/app_server.R, R/utils_qaqc.R) |

## Round 5 — current-version fixes (May 2026)

| Issue | How it was addressed |
|---|---|
| Tab 3 quick-set buttons not visible. | Done. The buttons existed but were hidden below a tall DT (the `pageLength = 20` setting pushed them past the fold). Moved both buttons out of `card_body` and into a `bslib::card_footer` so they remain visible regardless of how the table renders. Labels updated to IPCC-aligned wording: "Set all activity-data params to Normal ±15%" and "Set all coefficients to PERT". (R/app_ui.R) |
| Time-series correlation reports "no time-series data" when a built-in example is loaded. | Done. The example generators previously returned only the parameter table; the correlation auto-mode therefore had no data to compute from. Country X and Country Y now ship with five years of synthetic time-series (`generate_uganda_timeseries()`, `generate_country_y_timeseries()`). The correlation computation logic was extracted from `parse_uploaded_template()` into a shared helper `compute_corr_from_population()`, called both by the upload path and by `.load_example()`. The "no time-series" message was rewritten to mention that the built-in examples now ship with example time-series. (R/utils_ipcc_defaults.R, R/utils_template.R, R/app_server.R) |
| Trend tab requires a separate file upload instead of using the Parameter_TimeSeries already in the template. | Done. The Trend tab now reads the Parameter_TimeSeries sheet that's already in the main template (or in the built-in example) — same source as the correlation auto-mode. A new helper `trend_df_from_population()` reshapes the wide year × parameter table into the long format `run_trend_analysis()` expects, inheriting per-parameter `uncertainty_pct` from `rv$param_specs`. The CSV upload remains under an "Optional: override with separate CSV" expander for users who want to bring different trend data. The status panel reports "Using time-series from loaded template — N years × M parameters" before the run. (R/trend_tab.R, R/app_server.R, R/app_ui.R) |

## Round 6a — beta-tester pass 3 small UX fixes (May 2026)

| Issue | How it was addressed |
|---|---|
| #1 Home-tab analysis-mode default ticks one option, hiding the choice. | Done. `analysis_mode` now starts unticked (`selected = character(0)`); a yellow warning panel appears under the radio when nothing is picked, and the Tab 5 Run handler blocks and redirects back to Home if Run is clicked without a mode chosen. (R/app_ui.R, R/app_server.R) |
| #2 Tab 3 quick-set buttons cannot be undone — applying overwrites manual edits. | Done. Clicking the button snapshots `rv$param_specs` and applies the preset; clicking the same button again restores the snapshot. The label flips between "Set all activity-data params to Normal ±15%" and "Undo: restore previous values" so the user can see the toggle state. Editing any cell auto-clears the snapshot so subsequent clicks behave as fresh applies. (R/app_server.R) |
| #3 Tab 1 info-panel says "Choose your IPCC guidelines version" but there is no UI control to choose. | Done. Replaced the misleading sentence with: "this is read from `ipcc_version` in `Inventory_Metadata`." (R/app_ui.R) |
| #5 "Compare with/without correlations" checkbox is enabled even when no correlations exist. | Done. The checkbox is now rendered server-side. When both `corr_mode == "none"` and `ef_corr_mode == "none"`, the checkbox is wrapped in `style="opacity:0.55; pointer-events:none"` with a yellow why-disabled hint. (R/app_server.R, R/app_ui.R) |
| #6 After "← Back to settings" and re-running, results don't display directly. | Done. The Run handler now sets `rv$sim_view <- "results"` unconditionally on success (previously it was guarded by `if == "settings"`). Toast wording changed from "Scroll down to see results" to "Simulation complete — results displayed." (R/app_server.R, R/app_ui.R) |
| #7 Hero pills on Home are generic ("Tier 2 livestock", "Open source", etc.). | Done. Expanded from 4 to 6 IPCC-aligned pills: "Tier 2 · Approach 2", "Monte Carlo 10k+", "AD + coefficient", "CH4+N2O 5 sources", "Table 3.3 ready", "Sensitivity tornado". (R/app_ui.R) |
| #8 IPCC Report histograms appear flat; sensitivity tab shows "An error has occurred" Shiny crash. | Done. Histograms were reading `inv$enteric_ch4_total` from the cross-system inventory frame — those columns don't exist there (only the `total_*` aggregates do). Per-source vectors actually live in `by_system[[i]]$results$enteric_ch4_total`. Rewrote the renderer with a `.sum_across()` helper that sums per-system samples, skipping any zero-variance source with a placeholder. Tornado chart guarded against empty `rv$sensitivity`, missing `parameter` column, and zero rows so the generic Shiny crash is replaced by friendly placeholder annotations. (R/app_server.R) |

## Round 6b — auto-fill QA/QC card and Word run-summary export (May 2026)

| Issue | How it was addressed |
|---|---|
| #4 Auto-filled (imputed) parameters are easy to miss. Andreas asked QA/QC to report a dedicated "missing" category showing which IPCC default value and reference were used. | Done. The QA/QC `imputation` check is now emitted as `check = "missing_parameter"` with `status = "missing"` (a new severity ranked above `warn`) and a templated message: `"<param> not supplied in upload — auto-filled with IPCC default <value> <unit> (<ref>). Override in template if local data is available."`. Missing rows are sorted to the top of the QA/QC table, get an amber "missing" badge in the Summary card, and a new full-width "Auto-filled parameters" card appears above the QA/QC layout listing parameter, default value, unit, IPCC reference, and source for every imputed row. The parameter DT also gets a hover-tooltip on imputed rows. (R/utils_qaqc.R, R/app_server.R, R/app_ui.R) |
| #9 Reviewers want a single Word document at the end of a run that bundles all settings, results, sensitivity and graphs. | Done. New "Download Word summary" button on the IPCC Report tab. New module `R/utils_word_export.R` exposes `build_run_summary_docx()` which composes a `.docx` via `officer` + `flextable` containing: title block (date + IPCC version + region), run settings, auto-filled parameters list, headline results (mean, 95% CI, CV%, MoE% per source and total), IPCC Table 3.3, sensitivity ranking (top 10 SRC + PRCC), and three embedded ggplot2 charts (total CO2eq histogram, sensitivity tornado, per-source histogram grid). Doc is generated fresh each click from current `rv$*` state. ggplot was chosen over webshot to avoid the Chromium dependency on shinyapps.io. (R/utils_word_export.R, R/app_server.R, R/app_ui.R, app.R) |

## Round 7 — close every remaining Andreas comment (May 2026)

| Item | How it was addressed |
|---|---|
| **R1.12 + R1.13** Per-MMS `Frac_GasMS` and `Frac_LeachMS`. | Done. Manure_Management sheet extended from 14 to 22 columns. `mms_frac_defaults_2019()` helper provides per-MMS IPCC 2019 Table 10.22 / 10.23 defaults (12 manure systems). `calc_indirect_n2o_mm()` rewritten to accept `frac_gas_values` / `frac_leach_values` named vectors keyed by `mms_type`; old broadcast signature kept as a deprecated wrapper. Bridge in `app_server.R` extracts the new columns and threads them through `run_mc_simulation()`. EF4 / EF5 stay broadcast scalars (IPCC 2019 keeps them invariant by MMS). (R/utils_ipcc_defaults.R, R/utils_template.R, R/utils_validation.R, R/calc_manure_n2o.R, R/calc_ghg_master.R, R/mc_simulation.R, R/app_server.R) |
| **R1.14** Trend year-to-year correlation per IPCC 2019 §3.2.2.4. | Done. Three-way `radioButtons("year_corr", ...)` on Tab 9 with options `full` / `partial` / `none`, default = `full`. `run_trend_analysis()` accepts `year_corr` arg: `full` pre-samples coefficients once and reuses across years (AD redrawn fresh); `partial` builds AR(1) draws across years per coefficient with ρ=0.7; `none` keeps independent-year behaviour. New helpers `.pre_sample_coefficients()` and `.ar1_samples_one_coef()` in `R/trend_tab.R`. (R/trend_tab.R, R/app_ui.R, R/app_server.R, R/mc_sampling.R) |
| **R1.15** IPCC-guidance preset bug fix + new pair. | Done. Bug fix: preset matrix was being built on `activity_data` parameter names — after R1.6's rename only `N` is AD, so the matrix was 1×1 and the documented pairs (W↔MW, Milk↔Fat, …) silently never fired. Preset now operates on the unified parameter set via the new pure helper `build_ipcc_preset_corr(all_param_names)` in `R/mc_sampling.R`. Two new pairs: **Cfi ↔ Ca = 0.60** (structural breed/physiology pair) and **N ↔ W = 0.30** (cross-block via T4.3 unified copula). Final preset = 8 pairs covering 11 parameters. (R/mc_sampling.R, R/app_server.R, R/app_ui.R) |
| **T4.3** Unified 24-parameter copula (cross-block AD↔coefficient correlations). | Done. `generate_mc_samples()` accepts a new `unified_corr_matrix` arg holding AD-block + coefficient-block + cross-block entries. When supplied, sampling runs in one `MASS::mvrnorm()` pass over all parameters; otherwise falls back to the two-pass behaviour for back-compat. Bridge in `app_server.R` builds the unified matrix from `rv$corr_matrix` via `expand_corr_matrix(rv$corr_matrix, c(ad_names, coef_names))`. `decompose_uncertainty()` keeps freezing the opposite block via `distribution = "constant"` on the spec, which the unified sampler honours. End-to-end test confirms `cor(N, W) ≈ 0.30` when the preset is applied. (R/mc_sampling.R, R/mc_simulation.R, R/app_server.R, R/calc_ghg_master.R) |
| **T4.21** Dirichlet sampling on the MMS allocation simplex per IPCC 2019 Box 3.1A. | Done. New `sample_dirichlet_simplex(p, n_iter, names_vec, concentration)` helper in `R/mc_sampling.R` (gamma-quotient implementation — no `gtools` dependency). Bridge samples one `mms_fractions_matrix` per system group with α = `concentration` × stated proportions. `ghg_emissions_vec()` accepts the matrix and uses row i for iteration i instead of the broadcast scalar. New `numericInput("mms_concentration", ...)` on Tab 5: 0 = deterministic (pre-Round-7), 50 ≈ ±5pp jitter on a 50/50 split, higher = tighter. (R/mc_sampling.R, R/calc_ghg_master.R, R/mc_simulation.R, R/app_server.R, R/app_ui.R) |
| **R1.16** User-overridable `param_type` from the UI. | Done. Tab 3 `uncertainty_table` DT now persists edits (including `param_type`) back into `rv$param_specs` via a new `observeEvent(input$uncertainty_table_cell_edit, ...)` mirroring the bound-cascade rules of the Tab 1 DT. `param_type` edits validate against `c("activity_data", "coefficient")` (legacy `emission_factor` accepted) — invalid values are rejected with a notification toast. New defensive QA/QC check `param_type_invalid` flags any uploaded rows with non-canonical values. Downstream filters (preset builder, decomposition, trend tab) already read from the live spec, so overrides flow through automatically. (R/app_server.R, R/utils_qaqc.R) |
| **T2.1 expansion** Region-aware completeness defaults. | Done. `ensure_completeness()` accepts a `region` argument; before falling back to the global `PARAM_CATALOGUE$ipcc_default`, it consults the existing `IPCC_DEFAULTS_BY_REGION` lookup (5 parameters: W, Milk, DE, Ym, Bo across 5 regions) via `get_regional_default()`. The completeness call site in `app_server.R` passes `rv$inv_metadata$region`. Auto-fill message now reports "Region 'Africa' applied for: W, Milk, …" so the user sees the regional path was used. The `get_regional_default()` helper has existed since Round 3 G2 — Round 7 is just wiring it. (R/utils_validation.R, R/app_server.R) |
| **T0.1** Cosmetic wording pass. | Done. Tab 4 modal label `From template (auto)` → `From template (auto, time-series)` (matches Andreas's vocabulary). Tab 9 description rewritten to "Year-by-year inventory uncertainty: …". (R/app_ui.R) |

## Post-Round-7 hotfixes (May 2026)

| Issue | How it was fixed |
|---|---|
| **Legacy template upload reported "missing N" with no IPCC default.** Beta tester re-uploaded a pre-Round-4 example template (label still read "Eastern Uganda – pastoral", parameter names still `cattle_pop` / `live_weight` / etc.). Run failed with `"Cannot run: 1 core parameter(s) have no IPCC default and are missing: dairy / Eastern Uganda – pastoral / cows — N"`. | Done (commit `15668de`). Root cause: `parse_uploaded_template()` filtered rows by `params$parameter %in% PARAM_CATALOGUE$parameter` BEFORE applying `PARAM_ALIASES`. After the R1.6 rename the catalogue holds `N` / `W` / `Milk` / `DE`, so any row using the pre-rename spelling (`cattle_pop`, `live_weight`, `milk_yield`, `DE_pct`, …) got silently dropped on parse. The renamed `N` then looked missing to `ensure_completeness()`, which has no IPCC default for population, so the run failed with the confusing "missing N" error. Fix: swap the order — alias-translate first, then filter against the catalogue. Smoke test on a synthetic legacy template (5 rows, all old names) confirms all 5 rows survive parsing and translate to `N` / `W` / `Milk` / `DE` / `Cfi`; `ensure_completeness` returns valid with `N$mean = 500000` from the upload. (R/utils_template.R) |
| **IPCC Report tab "Sensitivity drivers" panel showed `An error has occurred. Check your logs or contact the app author for clarification.`** | Done (commit `a017e28`). Same bug pattern that landed in the Round 6b Word-export module. The tornado renderer used `sens <- rv$sensitivity$src %||% rv$sensitivity$prcc`. The custom `%||%` operator (R/utils_template.R:16) does `is.na(a[1])`, which on a data.frame returns a vector over the first column — that vector then can't be coerced to scalar logical inside the `||` chain, so the operator itself throws. Fix: replace `%||%` with explicit `if/else` checks for both data frames. Round 6a #8 had added defensive guards for empty / missing-column / zero-rows cases but missed this one. Grepped the rest of the codebase for the same `rv$<frame> %||% rv$<frame>` pattern — no other instances. (R/app_server.R) |
| **Audit follow-up: MMS dropdown wasn't actually version-aware.** During the post-v4c verification pass the user asked whether the IPCC 2019 / 2006 MMS dropdown comment (Blank Template #3) was really fixed. Found that the original Round 4 fix expanded `MMS_DEFAULTS` 8 → 12 and added a `validate_manure_sheet()` post-upload check, but kept a single combined dropdown — so a user opening the Excel template after picking "2019_refinement" in the metadata sheet still saw all 12 MMS options including 2006-only ones. Andreas's original ask was for the dropdown itself to filter, not for upload-time validation. | Done (Round 7.1). New radio "IPCC Guidelines version for template" on Tab 1 (`input$template_version`). `generate_template(ipcc_version = …)` plumbs through to a filtered `V_MMS <- get_mms_for_version(ipcc_version)$id`, the Inventory_Metadata `ipcc_version` cell pre-set, and a version-suffixed filename. Verified via dropdown-range trace: 2006 template's MMS validation references `'_Lists'!$E$2:$E$9` (8 entries), 2019 template references `'_Lists'!$E$2:$E$13` (12 entries). The post-upload `validate_manure_sheet()` check is retained as a defensive backstop. (R/utils_template.R, R/app_ui.R, R/app_server.R) |

---

## Summary

- **Done:** every comment in Andreas's review — Round 7 closed the previously-awaiting and v4.0-deferred items, plus two beta-tester hotfixes shipped on top.
- **Awaiting your input:** *(none)*.
- **Deferred to v4.x:** *(none in Andreas's scope)*. Future user-experience refinements that haven't been requested yet — surfacing the AR(1) ρ in trend partial mode as a UI slider; per-row MMS Dirichlet concentration override in the Manure_Management sheet; expert-elicitation correlation upload — would be reactive to feedback rather than driven by Andreas's review.

**Verification pass (May 2026):** every "Done" claim in this document was checked against the codebase. All 57 implementation claims verified TRUE — file paths, function names, line references, and behavioural details all match the live code. No silent omissions. Two post-Round-7 hotfixes (legacy template parser order, tornado `%||%` data-frame crash) added to the doc above as their own section.

Round 7 + hotfixes deployed live: https://mlolita26.shinyapps.io/cattle-ghg-uncertainty/.
